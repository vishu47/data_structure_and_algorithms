def minDist(arr, n, x, y):
    pass
    # dis = 0
    # track = len(arr)
    # flag = False
    # for num in arr:
    #     if x == num:

    # if track == len(arr):
    #     return -1
    # return track


x = [1, 3, 2]
a = 2
b = 1
cc = minDist(x, len(x), a, b)
print(cc)
